{
  "name": "emissary",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cd frontend && npx vite",
    "build": "cd frontend && npm run build",
    "postinstall": "cd frontend && npm install"
  }
}
